export enum ActionType {
    ADDNODE,
    DELETENODE,
    CONNECTNODES,
    DELETEEDGE,
    CHECKELEMENT,
    LOADDIAGRAM
}

export enum NodeType {
    Start,
    End, 
    Phase,
    Decision,
    SubWorkFlow,
}